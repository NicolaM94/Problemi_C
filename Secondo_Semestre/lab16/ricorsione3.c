/*Scrivere una funzione ricorsiva che stabilisce se
due numeri interi x e y sono primi tra loro (ossia non
hanno divisori comuni >1). La funzione deve restituire
1 (vero) o 0 (falso). Attenzione: esistono 2 casi base
(proprietà vera e proprietà falsa).*/

#include <stdio.h>

int verificaPrimi(a, b) {
    if
    if (a < b) {

    }
}

void main () {
    int a,b;

    scanf("%d %d", &a, &b);
    printf("%d", verificaPrimi());
}